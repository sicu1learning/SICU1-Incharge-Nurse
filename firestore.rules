rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all users with the app link
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
